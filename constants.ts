import { Invoice } from './types';

import { Client } from '@/types/dashboard';

export const INVOICES: Invoice[] = [];

export const CLIENTS: Client[] = [];

export const USER_NAME = "Brian Kimathi";
